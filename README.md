# Dados Nacionais por Amostra de Domicílios Simplificados (dnads)

## Installation

To install this package, you should use remotes (or devtools).

````R
# Install remotes
install.packages(remotes)

# Install dnads
remotes::install_github("hhmacedo/dnads")
````
